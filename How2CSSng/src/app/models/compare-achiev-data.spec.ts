import { CompareAchievData } from './compare-achiev-data';

describe('CompareAchievData', () => {
  it('should create an instance', () => {
    expect(new CompareAchievData()).toBeTruthy();
  });
});
